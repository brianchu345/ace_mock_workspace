# =============================================================================
# vcssimmpp_tool.cfg — VCS DV MPP simulation (re-uses vcssim tool)
# =============================================================================
# Identical tool to vcssim.  MPP differs only at flow level (MPP flags).
# =============================================================================
@include ../vcssim/vcssim_tool.cfg
