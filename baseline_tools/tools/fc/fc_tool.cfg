# =============================================================================
# fc_tool.cfg — Synopsys Fusion Compiler (RTL synthesis) tool configuration
# =============================================================================
[params]
activity = static

@include ../../global_config/params(activity)_tool_version.cfg

[envs]
FC_HOME = /p/hdk/cad/fusioncompiler/Toolversion(FC_VERSION)

[arc]
# NOT IN arc_license.txt — verify: synopsys/fusioncompiler
