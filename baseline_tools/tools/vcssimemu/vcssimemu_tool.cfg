# =============================================================================
# vcssimemu_tool.cfg — VCS DV emulation simulation (re-uses vcssim tool)
# =============================================================================
# Same tool setup as vcssim.  Emulation-specific paths (z_vcs / z_verdi)
# are typically set at the emulation platform (emu/zebu) level.
# =============================================================================
@include ../vcssim/vcssim_tool.cfg
