# =============================================================================
# questasimmpp_tool.cfg — QuestaSim MPP simulation (re-uses questasim tool)
# =============================================================================
# Identical tool setup to questasim.  MPP differs only at the flow/run level
# (multi-process-parallel flags handled by the flow wrapper).
# =============================================================================
@include ../questasim/questasim_tool.cfg
