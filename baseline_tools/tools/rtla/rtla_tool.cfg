# =============================================================================
# rtla_tool.cfg — Synopsys RTLA (RTL Architect) tool configuration
# =============================================================================
[params]
activity = static

@include ../../global_config/params(activity)_tool_version.cfg

[envs]
RTLA_HOME_PATH = /p/hdk/cad/rtla
RTLA_HOME      = Envs(RTLA_HOME_PATH)/Toolversion(RTLA_VERSION)

[arc]
# NOT IN arc_license.txt — verify: synopsys/rtla
# NOT IN arc_license.txt — verify: synopsys/dc
