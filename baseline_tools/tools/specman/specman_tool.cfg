# =============================================================================
# specman_tool.cfg — Cadence Specman-e DV verification tool configuration
# =============================================================================
[params]
activity = dv

@include ../../global_config/params(activity)_tool_version.cfg

[envs]
SPECMAN_DIR  = /p/hdk/rtl/cad/x86-64_linux26/cadence/specman/Toolversion(specman)
SPECMAN_HOME = Envs(SPECMAN_DIR)
VRST_HOME    = Envs(SPECMAN_DIR)
VRST_PATH    = Envs(SPECMAN_DIR)

[arc]
# NOT IN arc_license.txt — verify: cadence/specman
