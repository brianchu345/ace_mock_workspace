# =============================================================================
# h2b_tool.cfg — Synopsys H2B / LibAnalyzer (hard-boundary flow) tool config
# =============================================================================
[params]
activity = static

@include ../../global_config/params(activity)_tool_version.cfg

[envs]
ARC_HOME_PATH        = /p/hdk/pu_tu/prd/arc
ARC_HOME             = Envs(ARC_HOME_PATH)/Toolversion(ARC_BIN_VERSION)
LIBANALYZER_HOME_PATH = /p/hdk/cad/LibAnalyzer
LIBANALYZER_HOME      = Envs(LIBANALYZER_HOME_PATH)/Toolversion(LIB_ANALYZER_VERSION)
SYNOPSYS_DAS          = Envs(LIBANALYZER_HOME)
DAS_LOCAL_CACHE       = Envs(LIBANALYZER_HOME)/cache

[arc]
# NOT IN arc_license.txt — verify: synopsys/lynx
