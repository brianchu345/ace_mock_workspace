# =============================================================================
# coretools_tool.cfg — Synopsys CoreTools IP assembly toolkit
# =============================================================================
# Version base : tool_version.cfg (CORE_TOOLS_VERSION)
# Install base : HDK x86-64_linux30
# License      : synopsys/coretools
#
# NOTE: CORE_TOOLS_VERSION = TBD — not found in scanned Cheetah-RTL layers.
#       Verify with CoreTools owner. See migration_status.md Open Question #2.
# NOTE: Cheetah directory is `coretools`; user requested as `core_tools`.
#       ACE directory follows Cheetah naming convention: coretools.
# =============================================================================

[params]
activity = static

@include ../../global_config/params(activity)_tool_version.cfg

[params]
CORE_TOOLS_HOME_PATH = /p/hdk/rtl/cad/x86-64_linux30/synopsys/coretools

[envs]
CORE_TOOLS_DIR = Params(CORE_TOOLS_HOME_PATH)/Toolversion(CORE_TOOLS_VERSION)
CORE_TOOLS_VER = Toolversion(CORE_TOOLS_VERSION)

[arc]
# NOT IN arc_license.txt — verify: synopsys/coretools
