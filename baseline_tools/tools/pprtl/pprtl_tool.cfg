# =============================================================================
# pprtl_tool.cfg — Synopsys PrimePower RTL power analysis tool configuration
# =============================================================================
[params]
activity = static

@include ../../global_config/params(activity)_tool_version.cfg

[envs]
LC_PATH      = /p/hdk/cad/librarycompiler/Toolversion(LC_VERSION)
PP_RTLA_HOME = /p/hdk/cad/rtla/Toolversion(PP_RTLA_VERSION)
VERDI_HOME   = /p/hdk/rtl/cad/x86-64_linux26/synopsys/verdi3/Toolversion(VERDI_VERSION)
PP_HOME_PATH = /p/hdk/cad/primetime/Toolversion(PP_VERSION)

[arc]
# NOT IN arc_license.txt — verify: synopsys/primepower
# NOT IN arc_license.txt — verify: synopsys/rtla
