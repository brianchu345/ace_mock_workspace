# =============================================================================
# certitude_tool.cfg — Synopsys Certitude fault-grading tool configuration
# =============================================================================
# Version base : dv_tool_version.cfg (certitude = V-2023.12-SP2-2)
# Install base : HDK x86-64_linux44 (specific OS path for Certitude)
# License      : synopsys/vcsmx — Certitude borrows VCS license for its
#                simulation engine. Confirm this is still correct in ACE context
#                (see migration_status.md Open Question #5).
# CER_VCS_PLI  : PLI shared library bridging Certitude ↔ VCS simulation.
# runtools     : job dispatch framework required to submit Certitude regressions.
# =============================================================================

[params]
activity = dv

@include ../../global_config/params(activity)_tool_version.cfg

[envs]
CER_PATH     = /p/hdk/rtl/cad/x86-64_linux44/synopsys/certitude/Toolversion(certitude)
CER_VCS_PLI  = Envs(CER_PATH)/platforms/x86_Linux/xli.2.0/lib64/libcertess_pli_vcs.so
RUNTOOLS_PATH = /p/hdk/pu_tu/prd/runtools/master/Toolversion(runtools)

[arc]
# NOTE: Certitude uses VCS license as its simulation engine — verify token
# NOT IN arc_license.txt — verify: synopsys/vcsmx
