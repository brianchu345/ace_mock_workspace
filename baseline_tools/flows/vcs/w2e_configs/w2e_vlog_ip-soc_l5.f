# IP-SOC WG vlog level 5 (Critical Priority: not supported in future--no waivers)
#  generated by vcs_warn2err_xls2dotf.pl from VCS_CompileElabWarn_24ww04.xlsx on Thu Jan 25 09:32:22 PST 2024
  -error=BMII                                     # Bad message id
  -error=CHCPR                                    # Invalid Cross Definition
  -error=CIWC                                     # Ignored /* within /* */ comment
  -error=CNST-COD                                 # Constraint Option Deprecated
  -error=DCTL                                     # Decimal constant too large
  -error=DFLT_OPT                                 # Default option found
  -error=DPI-SFESN                                # Duplicate name in DPI
  -error=DUEMPTY                                  # $unit not empty
  -error=ECMDSMPD2                                # Mismatched method definition
  -error=EPIMD                                    # Empty Port in Module Declaration
  -error=FLVU                                     # For loop variable uninitialized
  -error=GCOIITB                                  # Guard condition on Illegal/Ignore transition bin
  -error=IPDASP                                   # Identifier in ANSI port declaration
  -error=IPDW                                     # Identifier previously declared
  -error=IRIID-W                                  # Inconsistent range in IO declare
  -error=IWU                                      # Implicit wire is used
  -error=NZIR                                     # Incorrect size number
  -error=OBSV2G                                   # Use of the -v2k_generate switch
  -error=OSPA-O                                   # Overriding same parameter again
  -error=PII                                      # Package importing itself
  -error=RIV                                      # Range ignored for variable
  -error=SV-CBMIE                                 # Curly brackets missing in 'inside' expr
  -error=SV-FTDI                                  # Forward 'typedef class' ignored
  -error=SV-IIS                                   # Illegal import statement
  -error=SV-PIU                                   # Package import statement in $unit scope
  -error=TMBIN                                    # Too many bits in Based Number
  -error=TMR                                      # Text macro redefined
  -error=UNKWN_IGOPTS                             # Unknown option(s) ignored
  -error=UODSBDNCAM                               # Default sequence not yet implemented
