# =============================================================================
# questa_flow.cfg — QuestaSim RTL compile+elab baseline flow configuration
# =============================================================================
@include ../../global_config/proj_base/sim_params/questa_params.cfg
@include ../../../cfg/$DUT.design.cfg
@include ../../global_config/proj_base/ip_type_params/ip_type.global_config(IP_TYPE).cfg

[questa]
PASS                  = gk
POST_PROCESSING_FILE  = $WORKAREA/baseline_tools/flows/questa/run_audit
ENABLE_RTL_DEPSLIST   = true
CREATE_OUTDIR_LINK    = false
MODEL_SIZE_REDUCTION  = $WORKAREA/baseline_tools/flows/questa/cleanup.cfg
VERILOG_ANALYZE_OPTS  = Params(ALTR_QUESTA_ANALYZE_SETTINGS) Params(ALTR_MACROS_SIMPWR_RTL)
ELAB_OPTS_SWITCH      = Params(ALTR_QUESTA_ELAB_SETTINGS)
